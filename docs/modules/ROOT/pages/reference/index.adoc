= Reference
:page-product: agent-memory
:toclevels: 2

Look up the contract for your SDK and backend. Start with xref:reference/backend-capabilities.adoc[Backend capabilities and data scope] when comparing Bolt and NAMS. These pages describe the checked source; verify availability in the SDK artifact you install.

// Retain former section URLs at the current related topic.
== Python API

anchor:_api_reference[]anchor:_extraction_reference[]anchor:_tools[]anchor:_installation[]

* xref:reference/api/adapters.adoc[Adapters reference] -- Constructor signatures for every LLM adapter shipped in `neo4j_agent_memory.llm.adapters`.
* xref:reference/api/factory.adoc[Provider factory reference] -- The `from_provider` string-shorthand factory for embedding and LLM providers.
* xref:reference/api/llm-provider.adoc[LLM provider API reference] -- Protocols, types, factory, and exception hierarchy in `neo4j_agent_memory.llm`.
* xref:reference/api/long-term.adoc[LongTermMemory API reference] -- Entity, preference, fact, and relationship operations exposed through `client.long_term`.
** xref:reference/api/long-term-entities.adoc[Entity and relationship operations] -- `add_entity`, `search_entities`, `add_relationship`, and the `Entity`/`Relationship` models.
** xref:reference/api/long-term-preferences.adoc[Preference and fact operations] -- `add_preference`, `search_preferences`, `add_fact`, and the `Preference`/`Fact` models.
** xref:reference/api/long-term-deduplication.adoc[Deduplication and provenance] -- Ingest-time duplicate detection and extraction provenance tracking.
** xref:reference/api/long-term-geospatial.adoc[Geospatial operations and NAMS behavior] -- Location search, geocoding, `get_context`, and the NAMS wrapper differences.
* xref:reference/api/memory-client.adoc[MemoryClient API reference] -- Connects to the configured backend and exposes its memory stores.
* xref:reference/api/reasoning.adoc[ReasoningMemory API reference] -- Trace, step, and tool-call operations exposed through `client.reasoning`.
** xref:reference/api/reasoning-traces.adoc[Trace operations] -- `start_trace`, `complete_trace`, `get_trace_with_steps`, and related lookups.
** xref:reference/api/reasoning-steps.adoc[Step, tool-call, and streaming operations] -- `add_step`, `record_tool_call`, and the `StreamingTraceRecorder` context manager.
** xref:reference/api/reasoning-search-stats.adoc[Search, statistics, and NAMS lifecycle] -- `get_similar_traces`, `get_tool_stats`, and the NAMS synthetic-trace lifecycle.
** xref:reference/api/reasoning-models.adoc[Models and structured outcomes] -- `ReasoningTrace`, `ReasoningStep`, `ToolCall`, and `TraceOutcome`.
* xref:reference/api/short-term.adoc[ShortTermMemory API reference] -- Conversation and message operations exposed through `client.short_term`.
** xref:reference/api/short-term-messages.adoc[Conversation and message operations] -- `add_message`, `add_messages_batch`, `get_conversation`, and the NAMS conversation operations.
** xref:reference/api/short-term-search.adoc[Message search and context] -- `search_messages` and `get_context`.
** xref:reference/api/short-term-sessions.adoc[Sessions, summaries, and maintenance] -- `list_sessions`, `get_conversation_summary`, `migrate_message_links`, and related maintenance calls.

// Retain former section URLs at the current related topic.
== Configuration, service and SDK contracts

anchor:_configuration[]anchor:_quick_links[]anchor:_minimal_example[]anchor:_reference_philosophy[]

=== Configuration and CLI

* xref:reference/backend-capabilities.adoc[Backend capabilities and data scope] -- Which operations and data are available on Bolt versus NAMS.
* xref:reference/configuration.adoc[Configuration reference] -- `MemorySettings` fields, defaults, precedence, and provider shapes.
** xref:reference/configuration-connections.adoc[Neo4j and NAMS connection settings] -- `Neo4jConfig` and `NamsConfig`.
** xref:reference/configuration-providers.adoc[Embedding, LLM, schema, extraction, and resolution settings] -- `EmbeddingConfig`, `LLMConfig`, `SchemaConfig`, `ExtractionConfig`, `ResolutionConfig`.
** xref:reference/configuration-operations.adoc[Memory, search, geocoding, and enrichment settings] -- `MemoryConfig`, `SearchConfig`, `GeocodingConfig`, `EnrichmentConfig`, and the integration-limits summary.
** xref:reference/configuration-advanced.adoc[Deduplication, observability, and CLI settings] -- The ingest-time `DeduplicationConfig`, the observability factory, and CLI configuration.
* xref:reference/environment-variables.adoc[Environment variables reference] -- Every `NAM_` environment variable `MemorySettings` and its integrations recognize.
* xref:reference/cli.adoc[CLI reference] -- Commands for local extraction, bolt schema/statistics, and MCP serving.
* xref:reference/mcp-tools.adoc[Self-hosted Python MCP reference] -- Tool parameters, resources, and prompts for the self-hosted MCP server.
* xref:reference/deployment.adoc[Documentation publication] -- How this documentation site is built and published.
* xref:reference/deploy-an-application.adoc[Deploy a memory application] -- Compares deployment paths for an application built on the SDKs.
* xref:reference/extractors.adoc[Extractor classes reference] -- Local Python extraction classes and result models.
** xref:reference/extractor-classes.adoc[Built-in extractor classes] -- `SpacyEntityExtractor`, `GLiNEREntityExtractor`, and `LLMEntityExtractor`.
** xref:reference/extractor-relations.adoc[Relation extractors] -- `GLiRELExtractor` and `GLiNERWithRelationsExtractor`.
** xref:reference/extractor-pipeline.adoc[Extraction pipeline] -- `ExtractionPipeline`, merge strategies, and the pipeline/batch result models.
** xref:reference/extractor-builder.adoc[Extractor builder] -- `ExtractorBuilder` fluent construction.
** xref:reference/extractor-streaming.adoc[Streaming extraction] -- `StreamingExtractor` for chunked long-document extraction.
* xref:reference/schemas.adoc[Domain schemas reference] -- Built-in domain schemas and custom schema creation.
** xref:reference/schemas-domains.adoc[Built-in domain schemas] -- The eight registered GLiNER schemas and how to select one.
** xref:reference/schemas-custom.adoc[Custom schemas and persistence] -- `DomainSchema`, the Bolt-only `SchemaManager`, and the GLiNER-to-POLE+O type mapping.
** xref:reference/schemas-api.adoc[SchemaManager persistence API] -- `save_schema`, `load_schema`, `list_schemas`, and the rest of the CRUD surface.
* xref:reference/schema-objects.adoc[Schema objects reference] -- Constraints, regular, vector, and point indexes the bolt backend creates on connection.

=== Hosted service

* xref:reference/nams-mcp.adoc[Hosted NAMS MCP server] -- The MCP server the hosted NAMS backend ships, independent of the SDK.
* xref:reference/authentication.adoc[Authentication and API keys] -- How NAMS authenticates requests and where API keys come from.
* xref:reference/rest-api.adoc[REST API reference] -- The NAMS REST API endpoints at `/v1`.
* xref:reference/nams-limits.adoc[NAMS limits and behavior] -- Operational limits, quotas, and runtime behavior for the hosted backend.
* xref:reference/ontology-api.adoc[Ontology API reference] -- NAMS-only ontology management; raises `NotSupportedError` on bolt.
* xref:reference/skills-api.adoc[Skills API (Preview)] -- The NAMS-hosted Agent Skills REST and MCP surface.

=== TypeScript

* xref:reference/typescript-api.adoc[TypeScript SDK API reference] -- The shape of `@neo4j-labs/agent-memory`'s public surface, linking into the TypeDoc reference.
