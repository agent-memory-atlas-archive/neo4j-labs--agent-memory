= Environment variables reference
:page-product: agent-memory
:page-pagination:
:toclevels: 2

Environment inputs recognized by Python MemorySettings and its optional integrations. All declared child fields are listed, including settings that the current MemoryClient does not automatically consume; those limits are stated explicitly.

This page is long; jump straight to a prefix instead of scrolling:

[cols="1,3",options="header"]
|===
| Section | Variable prefix
| <<_loading_and_precedence,Loading and precedence>> | `NAM_BACKEND`, `NAM_EMBEDDING`, `NAM_LLM`
| <<_nams_process_aliases,NAMS process aliases>> | `MEMORY_API_KEY`, `MEMORY_ENDPOINT`, `MEMORY_WORKSPACE_ID`
| <<_neo4j_connection,Neo4j connection>> | `NAM_NEO4J__*`
| <<_nams_connection,NAMS connection>> | `NAM_NAMS__*`
| <<_embedding_configuration,Embedding configuration>> | `NAM_EMBEDDING__*`
| <<_llm_configuration,LLM configuration>> | `NAM_LLM__*`
| <<_graph_schema_configuration,Graph schema configuration>> | `NAM_SCHEMA_CONFIG__*`
| <<_extraction_configuration,Extraction configuration>> | `NAM_EXTRACTION__*`
| <<_resolution_configuration,Resolution configuration>> | `NAM_RESOLUTION__*`
| <<_memory_behavior,Memory behavior>> | `NAM_MEMORY__*`
| <<_search_configuration,Search configuration>> | `NAM_SEARCH__*`
| <<_geocoding_configuration,Geocoding configuration>> | `NAM_GEOCODING__*`
| <<_enrichment_configuration,Enrichment configuration>> | `NAM_ENRICHMENT__*`
| <<_integration_limits,Integration limits>> | Declared fields the client does not apply automatically
| <<_cli_and_provider_environment,CLI and provider environment>> | `NEO4J_*`, `MCP_USER_ID`, provider SDK variables
| <<_example_dotenv_configuration,Example dotenv configuration>> | A complete `.env` example
| <<_test_harness_switches,Test harness switches>> | `RUN_INTEGRATION_TESTS` and related test-only switches
|===

anchor:_overview[]
anchor:_provider_strings[]

== Loading and precedence

`NAM_GROUP__FIELD` maps to `settings.group.field`. Constructor values override process environment, then `.env`, configured file secrets, and defaults. Arrays/dictionaries use JSON. Unknown nested fields in a recognized group are validation errors. See xref:reference/configuration.adoc#precedence[Configuration sources].

Use lowercase `true`/`false` for booleans and quote JSON lists in a shell. Do not mix a top-level provider string with nested fields for that same provider; use a constructed provider when you need its additional options.

[cols="2,2,4",options="header"]
|===
| Variable | Default | Behavior
| `NAM_BACKEND` | Unset | `bolt` or `nams`; explicit selection overrides auto-detection.
| `NAM_EMBEDDING` | Implicit OpenAI config | Provider string, for example `openai/text-embedding-3-small`; resolves through from_provider.
| `NAM_LLM` | Resolved from extraction requirements | Provider string, for example `openai/gpt-4o-mini`.
|===

== NAMS process aliases

These aliases are read from process environment after settings-source loading. Use the `NAM_NAMS__...` form in a dotenv file unless your application explicitly loads that file into process environment.

[cols="2,1,4",options="header"]
|===
| Alias | Default | Behavior
| `MEMORY_API_KEY` | Unset | Populates an unset NAMS key. A populated key selects NAMS when backend is not pinned.
| `MEMORY_ENDPOINT` | Unset | Overrides the default NAMS endpoint if endpoint was not explicitly set.
| `MEMORY_WORKSPACE_ID` | Unset | Populates an unset workspace ID, sent as X-Workspace-Id. Workspace access is determined by your credentials.
|===

[source,bash]
----
export MEMORY_API_KEY=nams_example_replace_with_your_key
export NAM_BACKEND=nams
export NAM_NAMS__TIMEOUT=60
----

== Neo4j connection

`NAM_NEO4J__` prefix. All scalar-valued; none need JSON encoding.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| `NAM_NEO4J__URI` | `bolt://localhost:7687` | Neo4j connection URI
| `NAM_NEO4J__USERNAME` | `neo4j` | Neo4j username
| `NAM_NEO4J__PASSWORD` | required | Neo4j password
| `NAM_NEO4J__DATABASE` | `neo4j` | Neo4j database name
| pass:[<code>NAM_<wbr>NEO4J__<wbr>MAX_<wbr>CONNECTION_<wbr>POOL_<wbr>SIZE</code>] | `50` | Maximum connection pool size
| pass:[<code>NAM_<wbr>NEO4J__<wbr>CONNECTION_<wbr>TIMEOUT</code>] | `30.0` | Connection timeout in seconds
| pass:[<code>NAM_<wbr>NEO4J__<wbr>MAX_<wbr>TRANSACTION_<wbr>RETRY_<wbr>TIME</code>] | `30.0` | Maximum transaction retry time in seconds
| pass:[<code>NAM_<wbr>NEO4J__<wbr>MAX_<wbr>CONNECTION_<wbr>LIFETIME</code>] | `300` | Maximum lifetime of a pooled connection in seconds
| pass:[<code>NAM_<wbr>NEO4J__<wbr>LIVENESS_<wbr>CHECK_<wbr>TIMEOUT</code>] | `60` | Seconds a connection can be idle before a liveness check
| `NAM_NEO4J__KEEP_ALIVE` | `true` | Enable TCP keep-alive on connections
|===

See xref:reference/configuration-connections.adoc#neo4j[Neo4j connection] for the Python kwarg names and constraints.

anchor:_nams[]

== NAMS connection

`+NAM_NAMS__+` prefix. `+NAM_NAMS__HEADERS+` is a JSON object, for example `+NAM_NAMS__HEADERS='{"X-Trace-Id": "abc"}'+`.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| `NAM_NAMS__ENDPOINT` | `https://memory.neo4jlabs.com/v1` | Base URL
| `NAM_NAMS__API_KEY` | unset | NAMS API key (`nams_...`); an unset value may be populated from `MEMORY_API_KEY`
| `NAM_NAMS__WORKSPACE_ID` | unset | Sent as `X-Workspace-Id` when set
| `NAM_NAMS__TIMEOUT` | `30.0` | HTTP request timeout in seconds
| `NAM_NAMS__MAX_RETRIES` | `3` | Max retry attempts for 429/5xx/network errors
| pass:[<code>NAM_<wbr>NAMS__<wbr>RETRY_<wbr>BACKOFF_<wbr>SECONDS</code>] | `0.5` | Base exponential backoff between retries
| `NAM_NAMS__HEADERS` | `{}` | Extra HTTP headers added to every request
| pass:[<code>NAM_<wbr>NAMS__<wbr>VALIDATE_<wbr>ON_<wbr>CONNECT</code>] | `true` | Probe an authenticated connection when connecting
| pass:[<code>NAM_<wbr>NAMS__<wbr>TRANSPORT_<wbr>MODE</code>] | `auto` | `auto`, `rest`, or `bridge` wire-protocol override
|===

See xref:reference/configuration-connections.adoc#nams[NAMS connection] for the full field table.

== Embedding configuration

`NAM_EMBEDDING__` prefix, legacy nested-provider settings — provider strings/instances use adapter-specific configuration instead (see xref:reference/api/adapters.adoc[Adapters]). All fields are scalar-valued.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| `NAM_EMBEDDING__PROVIDER` | `openai` | Embedding provider to use
| `NAM_EMBEDDING__MODEL` | `text-embedding-3-small` | Embedding model name
| pass:[<code>NAM_<wbr>EMBEDDING__<wbr>DIMENSIONS</code>] | `1536` | Embedding dimensions
| `NAM_EMBEDDING__API_KEY` | unset | API key for embedding provider
| pass:[<code>NAM_<wbr>EMBEDDING__<wbr>BATCH_<wbr>SIZE</code>] | `100` | Batch size for embeddings
| `NAM_EMBEDDING__DEVICE` | `cpu` | Device for sentence transformers (cpu/cuda)
| pass:[<code>NAM_<wbr>EMBEDDING__<wbr>PROJECT_<wbr>ID</code>] | unset | GCP project ID for Vertex AI
| `NAM_EMBEDDING__LOCATION` | `us-central1` | GCP region for Vertex AI
| pass:[<code>NAM_<wbr>EMBEDDING__<wbr>TASK_<wbr>TYPE</code>] | `RETRIEVAL_DOCUMENT` | Vertex AI task type
| pass:[<code>NAM_<wbr>EMBEDDING__<wbr>OUTPUT_<wbr>DIMENSIONALITY</code>] | unset (768 for Vertex AI) | Vertex AI output dimensionality
| pass:[<code>NAM_<wbr>EMBEDDING__<wbr>AWS_<wbr>REGION</code>] | unset | AWS region for Bedrock
| pass:[<code>NAM_<wbr>EMBEDDING__<wbr>AWS_<wbr>PROFILE</code>] | unset | AWS credentials profile name
|===

See xref:reference/configuration-providers.adoc#embedding[Embedding configuration] for the full field table.

== LLM configuration

`NAM_LLM__` prefix, legacy nested-provider settings — provider strings/instances use adapter-specific configuration instead. All fields are scalar-valued.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| `NAM_LLM__PROVIDER` | `openai` | LLM provider to use
| `NAM_LLM__MODEL` | `gpt-4o-mini` | LLM model name
| `NAM_LLM__API_KEY` | unset | API key for LLM provider
| `NAM_LLM__TEMPERATURE` | `0.0` | LLM temperature — not automatically applied by MemoryClient
| `NAM_LLM__MAX_TOKENS` | `4096` | Maximum tokens for LLM — not automatically applied by MemoryClient
|===

See xref:reference/configuration-providers.adoc#llm[LLM configuration] for the full field table and the `temperature` / `max_tokens` integration limit.

== Graph schema configuration

`+NAM_SCHEMA_CONFIG__+` prefix. `+NAM_SCHEMA_CONFIG__ENTITY_TYPES+` is a JSON array, for example `+NAM_SCHEMA_CONFIG__ENTITY_TYPES='["PERSON", "ORGANIZATION"]'+`.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| pass:[<code>NAM_<wbr>SCHEMA_<wbr>CONFIG__<wbr>MODEL</code>] | `poleo` | Schema model to use (`poleo`, `legacy`, `custom`) — not automatically applied by MemoryClient
| pass:[<code>NAM_<wbr>SCHEMA_<wbr>CONFIG__<wbr>ENTITY_<wbr>TYPES</code>] | unset | Custom entity types (overrides model default when model=custom)
| pass:[<code>NAM_<wbr>SCHEMA_<wbr>CONFIG__<wbr>ENABLE_<wbr>SUBTYPES</code>] | `true` | Whether to track entity subtypes — not automatically applied by MemoryClient
| pass:[<code>NAM_<wbr>SCHEMA_<wbr>CONFIG__<wbr>STRICT_<wbr>TYPES</code>] | `false` | Whether to reject unknown entity types — not automatically applied by MemoryClient
| pass:[<code>NAM_<wbr>SCHEMA_<wbr>CONFIG__<wbr>CUSTOM_<wbr>SCHEMA_<wbr>PATH</code>] | unset | Accepted path field; not automatically loaded by MemoryClient
|===

See xref:reference/configuration-providers.adoc#schema[Graph schema configuration] for the full field table and its integration limits.

== Extraction configuration

`+NAM_EXTRACTION__+` prefix. `+NAM_EXTRACTION__ENTITY_TYPES+` is a JSON array. There are no batch, streaming, or chunk-size fields in this settings group — those are method arguments, not environment variables.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>EXTRACTOR_<wbr>TYPE</code>] | `pipeline` | Type of entity extractor (`llm`/`gliner`/`spacy`/`pipeline`/`none`)
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>ENABLE_<wbr>SPACY</code>] | `true` | Enable spaCy in extraction pipeline
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>ENABLE_<wbr>GLINER</code>] | `true` | Enable GLiNER in extraction pipeline
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>ENABLE_<wbr>LLM_<wbr>FALLBACK</code>] | `true` | Enable LLM as fallback in pipeline
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>MERGE_<wbr>STRATEGY</code>] | `confidence` | Strategy for merging results from multiple extractors
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>FALLBACK_<wbr>ON_<wbr>EMPTY</code>] | `true` | Continue to next stage if current stage returns no results
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>SPACY_<wbr>MODEL</code>] | `en_core_web_sm` | spaCy model name
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>SPACY_<wbr>CONFIDENCE</code>] | `0.85` | Default confidence score for spaCy extractions
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>GLINER_<wbr>MODEL</code>] | `gliner-community/gliner_medium-v2.5` | GLiNER model name
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>GLINER_<wbr>THRESHOLD</code>] | `0.5` | GLiNER confidence threshold
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>GLINER_<wbr>DEVICE</code>] | `cpu` | Device for GLiNER model (cpu/cuda/mps)
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>GLINER_<wbr>SCHEMA</code>] | unset | Domain schema for GLiNER extraction (poleo, podcast, news, scientific, business, entertainment, medical, legal)
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>LLM_<wbr>MODEL</code>] | `gpt-4o-mini` | LLM model for extraction
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>ENTITY_<wbr>TYPES</code>] | `["PERSON", "ORGANIZATION", "LOCATION", "EVENT", "OBJECT"]` | Entity types to extract (POLE+O by default)
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>EXTRACT_<wbr>RELATIONS</code>] | `true` | Whether to extract relations
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>EXTRACT_<wbr>PREFERENCES</code>] | `true` | Whether to extract preferences
| pass:[<code>NAM_<wbr>EXTRACTION__<wbr>CONFIDENCE_<wbr>THRESHOLD</code>] | `0.5` | Minimum confidence threshold for extracted entities — not automatically applied by MemoryClient
|===

See xref:reference/configuration-providers.adoc#extraction[Extraction configuration] for the full field table.

== Resolution configuration

`+NAM_RESOLUTION__+` prefix. All scalar-valued. `+NAM_RESOLUTION__EMBEDDING_THRESHOLD+` and `+NAM_RESOLUTION__MATCH_SAME_TYPE_ONLY+` are not valid fields; the semantic threshold field is `+NAM_RESOLUTION__SEMANTIC_THRESHOLD+`.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| pass:[<code>NAM_<wbr>RESOLUTION__<wbr>STRATEGY</code>] | `composite` | Resolution strategy (`exact`/`fuzzy`/`semantic`/`composite`/`none`)
| pass:[<code>NAM_<wbr>RESOLUTION__<wbr>EXACT_<wbr>THRESHOLD</code>] | `1.0` | Exact match threshold
| pass:[<code>NAM_<wbr>RESOLUTION__<wbr>FUZZY_<wbr>THRESHOLD</code>] | `0.85` | Fuzzy match threshold
| pass:[<code>NAM_<wbr>RESOLUTION__<wbr>SEMANTIC_<wbr>THRESHOLD</code>] | `0.8` | Semantic match threshold
| pass:[<code>NAM_<wbr>RESOLUTION__<wbr>FUZZY_<wbr>SCORER</code>] | `token_sort_ratio` | Fuzzy matching scorer — not forwarded by MemoryClient
|===

See xref:reference/configuration-providers.adoc#resolution[Resolution configuration] for the full field table.

== Memory behavior

`NAM_MEMORY__` prefix. All fields are scalar-valued.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| pass:[<code>NAM_<wbr>MEMORY__<wbr>DEFAULT_<wbr>CONVERSATION_<wbr>LIMIT</code>] | `50` | Default conversation message limit — not automatically applied by MemoryClient
| pass:[<code>NAM_<wbr>MEMORY__<wbr>MESSAGE_<wbr>EMBEDDING_<wbr>ENABLED</code>] | `true` | Enable message embeddings — not automatically applied by MemoryClient
| pass:[<code>NAM_<wbr>MEMORY__<wbr>PREFERENCE_<wbr>CONFIDENCE_<wbr>THRESHOLD</code>] | `0.7` | Preference confidence threshold — not automatically applied by MemoryClient
| pass:[<code>NAM_<wbr>MEMORY__<wbr>FACT_<wbr>DEDUPLICATION_<wbr>ENABLED</code>] | `true` | Enable fact deduplication — not automatically applied by MemoryClient
| pass:[<code>NAM_<wbr>MEMORY__<wbr>TRACE_<wbr>EMBEDDING_<wbr>ENABLED</code>] | `true` | Enable reasoning trace embeddings — not automatically applied by MemoryClient
| pass:[<code>NAM_<wbr>MEMORY__<wbr>TOOL_<wbr>STATS_<wbr>ENABLED</code>] | `true` | Enable tool usage statistics — not automatically applied by MemoryClient
| pass:[<code>NAM_<wbr>MEMORY__<wbr>MULTI_<wbr>TENANT</code>] | `false` | Require `user_identifier` on the guarded bolt writes
| `NAM_MEMORY__WRITE_MODE` | `sync` | `sync` or `buffered` — controls `client.buffered.submit(...)`
| `NAM_MEMORY__MAX_PENDING` | `200` | Maximum in-flight buffered writes
| pass:[<code>NAM_<wbr>MEMORY__<wbr>CONVERSATION_<wbr>TTL_<wbr>DAYS</code>] | unset | TTL value for `archive_expired_conversations(older_than_days=...)` — not automatically applied
| `NAM_MEMORY__AUDIT_READ` | `false` | Audit preference for `consolidation.audit_reads(...)` — not automatically applied
|===

See xref:reference/configuration-operations.adoc#memory[Memory behavior] for the full field table, including `write_mode`/`max_pending` (buffered writes) and the integration limits on the remaining fields.

== Search configuration

`NAM_SEARCH__` prefix. All scalar-valued; none are automatically read by the memory stores.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| pass:[<code>NAM_<wbr>SEARCH__<wbr>DEFAULT_<wbr>LIMIT</code>] | `10` | Default search limit
| pass:[<code>NAM_<wbr>SEARCH__<wbr>DEFAULT_<wbr>THRESHOLD</code>] | `0.7` | Default similarity threshold
| pass:[<code>NAM_<wbr>SEARCH__<wbr>HYBRID_<wbr>SEARCH_<wbr>ENABLED</code>] | `true` | Enable hybrid search
| `NAM_SEARCH__GRAPH_DEPTH` | `2` | Graph traversal depth for search
|===

See xref:reference/configuration-operations.adoc#search[Search configuration] for the full field table.

== Geocoding configuration

`NAM_GEOCODING__` prefix. All fields are scalar-valued.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| `NAM_GEOCODING__ENABLED` | `false` | Enable automatic geocoding of Location entities
| `NAM_GEOCODING__PROVIDER` | `nominatim` | Geocoding provider to use (`nominatim`/`google`)
| `NAM_GEOCODING__API_KEY` | unset | API key for geocoding provider (required for Google)
| pass:[<code>NAM_<wbr>GEOCODING__<wbr>CACHE_<wbr>RESULTS</code>] | `true` | Cache geocoding results to avoid repeated API calls
| pass:[<code>NAM_<wbr>GEOCODING__<wbr>RATE_<wbr>LIMIT_<wbr>PER_<wbr>SECOND</code>] | `1.0` | Client request rate limit for the configured geocoder
| pass:[<code>NAM_<wbr>GEOCODING__<wbr>USER_<wbr>AGENT</code>] | `neo4j-agent-memory` | User-Agent sent by the configured geocoder
|===

See xref:reference/configuration-operations.adoc#geocoding[Geocoding configuration] for the full field table.

== Enrichment configuration

`+NAM_ENRICHMENT__+` prefix. `+NAM_ENRICHMENT__PROVIDERS+` and `+NAM_ENRICHMENT__ENTITY_TYPES+` are JSON arrays, for example `+NAM_ENRICHMENT__PROVIDERS='["wikimedia", "diffbot"]'+`.

[cols="4,2,4",options="header"]
|===
| Variable | Default | Meaning
| `NAM_ENRICHMENT__ENABLED` | `false` | Enable automatic entity enrichment
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>PROVIDERS</code>] | `["wikimedia"]` | Enrichment providers to use, in priority order
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>DIFFBOT_<wbr>API_<wbr>KEY</code>] | unset | API key for Diffbot Knowledge Graph
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>WIKIMEDIA_<wbr>RATE_<wbr>LIMIT</code>] | `0.5` | Seconds between Wikimedia API requests
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>DIFFBOT_<wbr>RATE_<wbr>LIMIT</code>] | `0.2` | Seconds between Diffbot API requests
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>CACHE_<wbr>RESULTS</code>] | `true` | Cache enrichment results to avoid repeated API calls
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>CACHE_<wbr>TTL_<wbr>HOURS</code>] | `168` | Hours to cache enrichment results
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>BACKGROUND_<wbr>ENABLED</code>] | `true` | Run enrichment in background (non-blocking)
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>QUEUE_<wbr>MAX_<wbr>SIZE</code>] | `1000` | Maximum enrichment queue size
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>MAX_<wbr>RETRIES</code>] | `3` | Maximum retry attempts for failed enrichments
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>RETRY_<wbr>DELAY_<wbr>SECONDS</code>] | `60.0` | Delay between retry attempts
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>ENTITY_<wbr>TYPES</code>] | `["PERSON", "ORGANIZATION", "LOCATION", "EVENT"]` | Entity types to enrich (empty = all types)
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>MIN_<wbr>CONFIDENCE</code>] | `0.7` | Minimum entity confidence to trigger enrichment
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>LANGUAGE</code>] | `en` | Preferred language for enrichment data
| pass:[<code>NAM_<wbr>ENRICHMENT__<wbr>USER_<wbr>AGENT</code>] | `neo4j-agent-memory/1.0` | User-Agent for API requests
|===

See xref:reference/configuration-operations.adoc#enrichment[Enrichment configuration] for the full field table.

anchor:integration-limits[]

anchor:_deduplication_configuration[]
anchor:_observability[]

== Integration limits

The declared field inventory above is not an assertion of runtime wiring. See xref:reference/configuration-operations.adoc#integration-limits[Configuration integration limits] for provider, method-default, schema, TTL, and auditing behavior. The settings family is `+NAM_RESOLUTION__SEMANTIC_THRESHOLD+`; `+NAM_RESOLUTION__EMBEDDING_THRESHOLD+` and `+NAM_RESOLUTION__MATCH_SAME_TYPE_ONLY+` are invalid fields.

There are no `+NAM_DEDUPLICATION__...+`, `+NAM_OBSERVABILITY__...+`, or `+NAM_CLI__...+` settings groups. Batch sizes/concurrency/chunk sizes for extraction belong to method arguments, not ExtractionConfig environment fields.

anchor:_aws_configuration[]

== CLI and provider environment

These inputs are consumed by command options or external provider adapters rather than becoming nested MemorySettings fields. Provider-specific availability depends on the installed extra.

[cols="2,3",options="header"]
|===
| Variable | Consumer
| `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD`, `NEO4J_DATABASE` | CLI connection options (database is an MCP serve option). The Python settings family instead uses NAM_NEO4J__... and USERNAME.
| `MCP_USER_ID` | MCP serve `--user-id` option.
| `NAM_LLM_API_KEY` | MCP serve LLM API-key override; not a MemorySettings field.
| `OPENAI_API_KEY` | OpenAI adapters/embedder when no explicit key is supplied.
| `ANTHROPIC_API_KEY` | Anthropic adapter when no explicit key is supplied.
| `AWS_REGION`, `AWS_DEFAULT_REGION`, `AWS_PROFILE` | Bedrock region/profile selection and AWS SDK configuration.
| `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_SESSION_TOKEN` | AWS SDK credential chain.
| pass:[<code>GOOGLE_<wbr>APPLICATION_<wbr>CREDENTIALS</code>] | Google SDK application-default credentials when using Vertex AI.
| `NAM_GEOCODING__API_KEY` | Google geocoder key. There is no automatic GOOGLE_GEOCODING_API_KEY fallback; pass that application variable explicitly if you use it.
| `OPIK_API_KEY` | Opik SDK credentials for the configured tracer.
| pass:[<code>OTEL_<wbr>EXPORTER_<wbr>OTLP_<wbr>ENDPOINT</code>] | External OpenTelemetry exporter convention. The SDK tracer requires an explicit endpoint argument to create an exporter; the variable alone does not enable export.
|===

The CLI does not automatically load `.neo4j-memory.yaml`. Review xref:reference/cli.adoc[CLI reference] for exact flags, including NAMS backend options.

anchor:_example_configuration[]

== Example dotenv configuration

Copy the credentials from xref:tutorials/first-agent-memory.adoc#_step_2_set_up_neo4j[the Aura connection setup].

This example uses the SDK's native `NAM_NEO4J__...` fields; the tutorial's helper explicitly maps its `NEO4J_*` variables to these settings. Plain `NEO4J_*` keys are not automatic `MemorySettings` aliases.

[source,bash]
----
NAM_BACKEND=bolt
NAM_NEO4J__URI="neo4j+s://<instance-id>.databases.neo4j.io"
NAM_NEO4J__USERNAME=neo4j
NAM_NEO4J__PASSWORD=replace-with-your-Aura-password
NAM_NEO4J__DATABASE=neo4j
NAM_EXTRACTION__EXTRACTOR_TYPE=pipeline
NAM_EXTRACTION__ENABLE_SPACY=true
NAM_EXTRACTION__ENABLE_GLINER=true
NAM_EXTRACTION__ENABLE_LLM_FALLBACK=false
NAM_EXTRACTION__GLINER_SCHEMA=podcast
NAM_ENRICHMENT__ENABLED=false
NAM_GEOCODING__ENABLED=false
----

anchor:_testing[]

== Test harness switches

Test-only inputs such as `RUN_INTEGRATION_TESTS`, `SKIP_INTEGRATION_TESTS`, `AUTO_START_DOCKER`, and `AUTO_STOP_DOCKER` belong to the test harness, not the SDK configuration API. Read the relevant test fixture before relying on their parsing or defaults.

== See also

* xref:reference/configuration.adoc[Python configuration]
* xref:how-to/use-nams.adoc[Use NAMS]
* xref:reference/cli.adoc[CLI options]
